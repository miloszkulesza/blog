import { TextShortPipe } from './text-short-pipe';

describe('TextShortPipe', () => {
  it('create an instance', () => {
    const pipe = new TextShortPipe();
    expect(pipe).toBeTruthy();
  });
});
